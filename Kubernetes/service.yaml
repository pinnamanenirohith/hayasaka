apiVersion: v1
kind: Service
metadata:
  name: hayasaka-service
  namespace: music-app
spec:
  type: NodePort
  selector:
    app: hayasaka
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
